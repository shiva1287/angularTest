import { SuccessInterceptor } from './success-interceptor';

describe('SuccessInterceptor', () => {
  it('should create an instance', () => {
    expect(new SuccessInterceptor()).toBeTruthy();
  });
});
